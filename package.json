{
  "name": "my-speedtest",
  "version": "1.0.0",
  "private": true,
  "description": "Bandwidth and latency tester between this server and the requesting browser. Zero runtime dependencies.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  }
}
